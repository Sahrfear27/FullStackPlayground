/**
 There are two types of numbers. The regular numbers which is of 64 bit
 The BigInt which is of abiturary length
 * **/

//  Convert a number into a string
let num = 225;
console.log(num.toString(16));
